name: deploying the backend

on:
  push:
    branches: [ main ]


jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2

      - name: Login to docker
        uses: docker/login-action@v2
        with: 
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: build the image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./dockerfile/Dockerfile.backend
          push: true
          tags: zeroskilldev/todo-app-backend:${{ github.sha }}

      - name: Execute remote SSH commands using SSH key
        uses: appleboy/ssh-action@v1
        with:
          host: 32.236.85.226 
          username: ubuntu
          key: ${{ secrets.KEY }}
          port: 22
          script: |
            docker stop todo-app-backend || true
            docker rm todo-app-backend || true

            docker pull zeroskilldev/todo-app-backend:${{ github.sha }}

            docker run -d \
              --name todo-app-backend \
              -p 3001:3001 \
              -e DATABASE_URL=postgresql://neondb_owner:npg_4SKF7pafbTBo@ep-damp-union-appcuqti-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
              zeroskilldev/todo-app-backend:${{ github.sha }}